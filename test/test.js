var assert = require('assert');
var TimeTrialMode = require('../src/timeTrial.js');
describe('TimeTrialMode', function() {
  describe('new', function() {
    it('should create a new instance of timeTrialMode', function() {
      assert.equal([1,2,3].indexOf(4), -1)
    })

    it('should return the value index when the value is present', function() {
      assert(TimeTrialMode, "New instance of class was not created.")
    })

    it('should be written')
  })
})
